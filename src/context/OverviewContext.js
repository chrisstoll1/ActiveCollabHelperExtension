import {createContext } from 'react';

export const SortDirection = createContext();
export const SetSortDirection = createContext();
export const SortOption = createContext();
export const SetSortOption = createContext();
